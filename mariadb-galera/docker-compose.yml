# Manual-QA lab: the siem-tracker app running against a 3-node MariaDB Galera
# cluster fronted by MaxScale. Mirrors the app repo's containers/mariadb-galera
# dev lab exactly in topology, but every service here is a PULLED registry
# image (ngmaibulat/usiem-tracker:latest for the app, stock upstream images
# for everything else) — this file never builds. The app's DATABASE_URL
# points at the MaxScale readwritesplit listener, so unlike the other
# replication labs it isn't pinned to one physical node. No nginx/squid/TLS
# in this lab (DB-topology-focused, not prod-shaped) — see ../default for
# that. Fully independent of the other labs in this repo: own ports, own
# volumes, own compose project name.
#
# Bring up (from this directory):
#   mkdir -p data/mariadb-logs data/maxscale-logs
#   cp example.env .env && docker compose pull && docker compose run --rm migrate && docker compose up -d
# App: http://localhost:3005 — MaxScale GUI: http://localhost:18989 (admin/mariadb)
name: mariadb-galera-lab

services:
  app:
    image: ngmaibulat/usiem-tracker:latest
    container_name: siem-tracker-galera-app
    depends_on:
      redis:
        condition: service_started
      mailhog:
        condition: service_started
      # No depends_on on maxscale/the mariadb nodes: they take much longer to
      # become usable than "started", and the app's own `restart:
      # unless-stopped` + startup check already retries until the DB answers.
    ports:
      - "3005:3000"
    environment:
      DATABASE_URL: mysql://siem:${MARIADB_PASSWORD:-siem_password}@maxscale:4006/siem_source_tracker
      # CONTROL_DATABASE_URL is left unset on purpose: the control plane
      # derives from the same MariaDB backend as siem_source_tracker_control
      # (created by init/01-init.sh), keeping this lab focused on its own
      # Galera/MaxScale topology, same as the source dev lab.
      REDIS_URL: redis://redis:6379
      SMTP_URL: smtp://mailhog:1025?from=noreply@siem-tracker.local
      APP_SECRET: ${APP_SECRET}
      APP_URL: http://localhost:3005
      TARGET_DATE: ${TARGET_DATE:-2026-12-31}
      # /admin/logs tabs mariadb-node1..3 / maxscale read from these — see
      # src/lib/serverLogs.ts. Bind-mounted below from the same host dirs the
      # mariadb/maxscale services write into.
      MARIADB_LOG_DIR: /app/mariadb-logs
      MAXSCALE_LOG_DIR: /app/maxscale-logs
      # FR-42/43 restore-helper only (pg_restore needs a real Postgres to
      # stage into) — not wired to CONTROL_DATABASE_URL by default here.
      RESTORE_PG_URL: postgresql://postgres:${POSTGRES_PASSWORD:-postgres_password}@postgres:5432/postgres
    volumes:
      - ./data/mariadb-logs:/app/mariadb-logs:ro
      - ./data/maxscale-logs:/app/maxscale-logs:ro
    restart: unless-stopped

  redis:
    image: redis:8-alpine
    container_name: siem-tracker-galera-redis
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - redis_data:/data
    restart: unless-stopped

  mailhog:
    image: mailhog/mailhog:latest
    container_name: siem-tracker-galera-mailhog
    ports:
      - "8030:8025"
    restart: unless-stopped

  # Topology: 3 mariadb nodes replicate via Galera (config ./galera.cnf);
  # MaxScale (./maxscale.cnf) fronts them with a readwritesplit listener on
  # :4006. node1 bootstraps the cluster when its grastate.dat is absent (fresh
  # volume) or marked safe_to_bootstrap — node2/3 depend on node1, so a
  # graceful `down` stops node1 LAST and leaves it safe to bootstrap. After an
  # UNGRACEFUL full-stack kill all nodes may hold safe_to_bootstrap: 0; recover
  # by starting node1 once with the flag forced:
  #   docker compose run --rm mariadb-node1 sh -c \
  #     "sed -i 's/safe_to_bootstrap: 0/safe_to_bootstrap: 1/' /var/lib/mysql/grastate.dat" \
  #   && docker compose up -d
  mariadb-node1:
    image: mariadb:11.8
    container_name: siem-tracker-galera-node1
    environment:
      MARIADB_ROOT_PASSWORD: ${MARIADB_ROOT_PASSWORD:-root_password}
      MARIADB_DATABASE: siem_source_tracker
      MARIADB_USER: siem
      MARIADB_PASSWORD: ${MARIADB_PASSWORD:-siem_password}
    entrypoint:
      - sh
      - -c
      - >-
        touch /var/log/mysql/node1.err &&
        chown mysql:mysql /var/log/mysql/node1.err &&
        chmod 755 /var/log/mysql && chmod 644 /var/log/mysql/node1.err;
        BOOT="";
        if [ ! -f /var/lib/mysql/grastate.dat ] ||
        grep -q "safe_to_bootstrap: 1" /var/lib/mysql/grastate.dat;
        then BOOT=--wsrep-new-cluster; fi;
        exec docker-entrypoint.sh mariadbd $$BOOT
        --wsrep_node_name=node1 --wsrep_node_address=mariadb-node1
        --log_error=/var/log/mysql/node1.err
    ports:
      - "3346:3306"
    volumes:
      - ./galera.cnf:/etc/mysql/conf.d/galera.cnf:ro
      # Runs once on a fresh volume (bootstrap node only): creates the control
      # database, the maxscale user, and a lab_demo sandbox database;
      # replicated to the others.
      - ./init:/docker-entrypoint-initdb.d:ro
      - node1_data:/var/lib/mysql
      - ./data/mariadb-logs:/var/log/mysql
    healthcheck:
      # Root credentials, not the image's healthcheck.sh: SST overwrites the
      # joiners' mysql.user with the donor's, orphaning their locally generated
      # healthcheck@localhost passwords. Root's password is identical
      # cluster-wide and survives SST. A non-synced galera node rejects
      # queries, so `SELECT 1` doubles as a synced-check.
      test: ["CMD-SHELL", "mariadb -h127.0.0.1 -uroot -p\"$$MARIADB_ROOT_PASSWORD\" -e 'SELECT 1' >/dev/null 2>&1"]
      interval: 5s
      timeout: 5s
      retries: 30
    restart: unless-stopped

  mariadb-node2:
    image: mariadb:11.8
    container_name: siem-tracker-galera-node2
    environment:
      MARIADB_ROOT_PASSWORD: ${MARIADB_ROOT_PASSWORD:-root_password}
    entrypoint:
      - sh
      - -c
      - >-
        touch /var/log/mysql/node2.err &&
        chown mysql:mysql /var/log/mysql/node2.err &&
        chmod 755 /var/log/mysql && chmod 644 /var/log/mysql/node2.err;
        exec docker-entrypoint.sh mariadbd
        --wsrep_node_name=node2 --wsrep_node_address=mariadb-node2
        --log_error=/var/log/mysql/node2.err
    ports:
      - "3347:3306"
    volumes:
      - ./galera.cnf:/etc/mysql/conf.d/galera.cnf:ro
      - node2_data:/var/lib/mysql
      - ./data/mariadb-logs:/var/log/mysql
    depends_on:
      mariadb-node1:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "mariadb -h127.0.0.1 -uroot -p\"$$MARIADB_ROOT_PASSWORD\" -e 'SELECT 1' >/dev/null 2>&1"]
      interval: 5s
      timeout: 5s
      retries: 30
    restart: unless-stopped

  mariadb-node3:
    image: mariadb:11.8
    container_name: siem-tracker-galera-node3
    environment:
      MARIADB_ROOT_PASSWORD: ${MARIADB_ROOT_PASSWORD:-root_password}
    entrypoint:
      - sh
      - -c
      - >-
        touch /var/log/mysql/node3.err &&
        chown mysql:mysql /var/log/mysql/node3.err &&
        chmod 755 /var/log/mysql && chmod 644 /var/log/mysql/node3.err;
        exec docker-entrypoint.sh mariadbd
        --wsrep_node_name=node3 --wsrep_node_address=mariadb-node3
        --log_error=/var/log/mysql/node3.err
    ports:
      - "3348:3306"
    volumes:
      - ./galera.cnf:/etc/mysql/conf.d/galera.cnf:ro
      - node3_data:/var/lib/mysql
      - ./data/mariadb-logs:/var/log/mysql
    depends_on:
      mariadb-node1:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "mariadb -h127.0.0.1 -uroot -p\"$$MARIADB_ROOT_PASSWORD\" -e 'SELECT 1' >/dev/null 2>&1"]
      interval: 5s
      timeout: 5s
      retries: 30
    restart: unless-stopped

  # SQL proxy in front of the Galera nodes: readwritesplit listener on 4006,
  # galeramon monitor, REST API/GUI on 8989 (dev/QA-only plain HTTP, admin/mariadb).
  maxscale:
    image: mariadb/maxscale:24.02
    container_name: siem-tracker-galera-maxscale
    entrypoint:
      - sh
      - -c
      - >-
        mkdir -p /var/log/maxscale &&
        touch /var/log/maxscale/maxscale.log &&
        chown -R maxscale:maxscale /var/log/maxscale &&
        chmod 755 /var/log/maxscale && chmod 644 /var/log/maxscale/maxscale.log &&
        exec maxscale -d -U maxscale -f /etc/maxscale.cnf
    ports:
      - "14006:4006"   # mysql://…@localhost:14006/…
      - "18989:8989"   # MaxScale GUI/REST (browser: http://localhost:18989, admin/mariadb)
    volumes:
      - ./maxscale.cnf:/etc/maxscale.cnf:ro
      - ./data/maxscale-logs:/var/log/maxscale
    depends_on:
      mariadb-node1:
        condition: service_healthy
      mariadb-node2:
        condition: service_started
      mariadb-node3:
        condition: service_started
    restart: unless-stopped

  # FR-42/43 restore-helper: NOT part of the app's normal data path — used only
  # to stage legacy pg_dump restores (pg_restore's temp database) before
  # porting the data into the MariaDB cluster.
  postgres:
    image: postgres:18
    container_name: siem-tracker-galera-postgres
    environment:
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres_password}
    ports:
      - "5445:5432"
    volumes:
      - postgres_data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 10
    restart: unless-stopped

  # One-shot schema migration (the app image splits this out of normal
  # startup — see docker-entrypoint.sh's `migrate` mode). Idempotent.
  migrate:
    image: ngmaibulat/usiem-tracker:latest
    container_name: siem-tracker-galera-migrate
    profiles: ["migrate"]
    # Unlike the app service, this one-shot job has no retry loop of its own,
    # so — unlike app's deliberately dependency-free depends_on — it DOES need
    # to wait for the cluster + MaxScale to actually be reachable before
    # `prisma migrate deploy` runs (same dependency chain as the maxscale
    # service itself).
    depends_on:
      mariadb-node1:
        condition: service_healthy
      mariadb-node2:
        condition: service_started
      mariadb-node3:
        condition: service_started
      maxscale:
        condition: service_started
    environment:
      DATABASE_URL: mysql://siem:${MARIADB_PASSWORD:-siem_password}@maxscale:4006/siem_source_tracker
    command: ["migrate"]
    restart: "no"

volumes:
  node1_data:
  node2_data:
  node3_data:
  postgres_data:
  redis_data:
